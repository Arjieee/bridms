{
  "name": "brgy-puerto-relief-system-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Barangay Puerto Relief Inventory and Distribution Monitoring System (Monorepo)",
  "scripts": {
    "dev": "node -e \"console.log('To start frontend and backend:\\n  npm run dev:frontend\\n  npm run dev:backend')\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install && npm --prefix backend install"
  }
}
